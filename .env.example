# Copy to .env and set credentials for this machine.
# All benchmark, endpoint, model, sweep, storage, build and server settings
# come from configs/*.json. Environment variables do not override them.

# Required to populate embedding caches.
# OPENAI_API_KEY=

# Optional credentials for the judge and extraction endpoints in JSON.
JUDGE_LLM_API_KEY=token-abc123
# EXTRACTION_LLM_API_KEY=

# Optional Hugging Face token for gated model downloads.
# HF_TOKEN=

# Optional local Qdrant deepcopy diagnostics; disabled when unset.
# Memory runs report primary and entity-store copies separately.
# Entity durations include both the worker sum and wall time with overlap counted once.
# QDRANT_PROFILE_DEEPCOPY=1

# Optional Mem0 retrieval-stage diagnostics for memory accuracy/latency runs.
# Apply the same flag to both Jasper and Qdrant comparison runs.
# Combine with QDRANT_PROFILE_DEEPCOPY=1 for copy durations within payload access.
# Timings are elapsed durations; nested stages and overlapping workers must not be added twice.
# MEM0_PROFILE_RETRIEVAL=1
